# ルートで: pip install pre-commit && pre-commit install
# 全ファイル: pre-commit run --all-files
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.4
    hooks:
      - id: ruff
        files: ^backend/.*\.py$
      - id: ruff-format
        files: ^backend/.*\.py$
